---
description: Colab 运行后自动将 data/ 结果写回本地项目目录
globs: notebooks/**/*.ipynb
alwaysApply: true
---

# OmniScreen Colab → 本地同步规则

本项目使用 **Cursor + Colab 内核** 运行 notebook。Colab 在云端执行，无法直接写入本地磁盘。

## 每次 `notebook_run_cell` 之后必须执行

1. 检查 cell 输出是否包含 `__OMNISCREEN_SYNC_START__` 标记
2. 若有，运行同步脚本写回本地：

```bash
python3 .cursor/colab_sync.py <(echo 'OUTPUT') /Users/schmit/Documents/OmniScreen-AI
```

或解析 MCP `notebook_run_cell` 返回的 markdown 文本，调用 `.cursor/colab_sync.py` 的逻辑写入 `data/`。

3. 同步完成后向用户确认写入了哪些文件

## 若模块未自动 export

运行 notebook 中任意包含 `export_for_local_sync()` 的 cell，或执行：

```python
export_for_local_sync()  # 导出全部 data/ 下文件
```

## 本地目标路径

`/Users/schmit/Documents/OmniScreen-AI/data/`
